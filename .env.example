# Supabase Configuration (OPTIONAL)
# These are only needed if you want to use Room Sharing features in Boss Time Tracker
# The app works perfectly fine without these - it will just use local storage instead
#
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# For local development:
# 1. Copy this file to .env.local
# 2. Replace the values above with your actual Supabase credentials
# 3. Restart the dev server (npm run dev)

# For production (Vercel):
# 1. Go to your Vercel project dashboard
# 2. Settings > Environment Variables
# 3. Add both variables with your actual values
# 4. Redeploy your project
